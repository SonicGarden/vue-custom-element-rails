RSpec.describe Vue::Custom::Element::Rails do
  it "has a version number" do
    expect(Vue::Custom::Element::Rails::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
